#include<stdio.h>
#include<stdlib.h>
#include<string.h>
//memory allocation

int main(){
	char *lista[3]];
	int cont;
	char aux[20];
	for(cont=0;cont<3;cont++){
		printf("palabra %d: ");
		scanf("%s",aux);
		longitud=strlen(aux);
		malloc(longitud);
	}
	
	
	return 0;
}
